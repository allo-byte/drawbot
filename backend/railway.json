{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd .. && npm run build && cd backend"
  },
  "deploy": {
    "startCommand": "bun run server.ts",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}